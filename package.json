{
  "name": "bramble-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static marketing site for Bramble. Pages are hand-written HTML plus blog pages generated by build.mjs; Vercel serves the committed output as-is. The only runtime dependency is @vercel/functions, used solely by middleware.js for IP-based EEA/UK detection.",
  "scripts": {
    "generate": "node build.mjs"
  },
  "dependencies": {
    "@vercel/functions": "^3.7.5"
  }
}
