# macOS
.DS_Store

# Dependencies
node_modules/

# Vercel
.vercel/

# Full-res/pre-retouch screenshot originals — keep local, don't publish
img/originals/

# AI-agent tooling (impeccable skill + hooks) — local dev only, never deploy
.claude/
.impeccable/

# Internal design/brand context docs — public repo is served at root, keep these local
PRODUCT.md
DESIGN.md
